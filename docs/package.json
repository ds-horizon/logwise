{
    "name": "logwise-docs",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vitepress dev --port 3000",
        "build": "vitepress build",
        "preview": "vitepress preview --port 3000"
    },
    "devDependencies": {
        "vitepress": "^1.5.0"
    }
}