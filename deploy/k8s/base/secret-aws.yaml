apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: logwise
type: Opaque
stringData:
  # Fill these with your actual credentials or rely on cloud IAM roles where possible.
  # For Git-committed manifests, keep these as placeholders.
  accessKeyId: "REPLACE_ME"
  secretAccessKey: "REPLACE_ME"
  sessionToken: "REPLACE_ME"


